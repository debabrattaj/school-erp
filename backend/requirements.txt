fastapi
uvicorn
sqlalchemy
pydantic
python-multipart
python-jose
argon2-cffi
python-dotenv
alembic>=1.13

# Optional: only needed to run under a WSGI host (e.g. cPanel/Passenger via
# passenger_wsgi.py) instead of uvicorn directly - see DEPLOYMENT_CPANEL.md
a2wsgi>=1.10

# Optional: enable Postgres (set DEFAULT_SCHOOL_DATABASE_URL/CENTRAL_DATABASE_URL to postgresql+psycopg://...)
psycopg[binary]>=3.1
reportlab>=4.0
qrcode[pil]>=7.4
pytest>=8.0
httpx>=0.27

# Optional: powers the chatbot's AI fallback (set ANTHROPIC_API_KEY to enable)
anthropic>=0.68
